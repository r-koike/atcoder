#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
	cin.tie(0);ios::sync_with_stdio(false);
	/* ====================================== */
	int N, M;
	cin >> N >> M;
	vector<vector<int>> a(N);
	for(int i = 0; i < N; i++){
		a[i].resize(M);
		for(int j = 0; j < M; j++){
			cin >> a[i][j];
		}
	}




	cout << ;
	/* ====================================== */
	return 0;
}